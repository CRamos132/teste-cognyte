import styled from 'styled-components'

const MapWrapper = styled.div`
    height: 450px;
    min-width: 80%;
    max-width: 500px;
    position: relative;
    margin: 16px;
`

export default MapWrapper